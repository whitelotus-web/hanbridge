# Base URL of the HanBridge API. On a physical device use your machine's LAN IP
# (e.g. http://192.168.1.10:8000); localhost only works in the iOS simulator.
EXPO_PUBLIC_API_BASE_URL=http://localhost:8000
